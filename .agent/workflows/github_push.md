---
description: Push changes to GitHub
---

1. Check the status of the repository: `git status`
2. Add all changes to the staging area: `git add .`
3. Commit the changes. Ask the user for a commit message if one wasn't provided.
4. Push the changes to the remote repository: `git push`
